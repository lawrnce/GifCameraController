source 'https://github.com/CocoaPods/Specs.git'
use_frameworks!

target 'GifCameraController_Example', :exclusive => true do
  pod 'GifCameraController', :path => '../'
end

target 'GifCameraController_Tests', :exclusive => true do
  pod 'GifCameraController', :path => '../'

  pod 'Quick', '~> 0.8'
  pod 'Nimble', '~> 3.0'
end
